version: "3.8"
services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:5001"  # App port
      - "9229:9229"  # Debug port (optional)
    volumes:
      - ./:/app
      - /app/tmp  # Exclude tmp directory from volume mount
    environment:
      # - GO_ENV=development
      - PORT=5001
    # depends_on:
    #   - postgres
    #   - redis
    networks:
      - app-network
    restart: unless-stopped

  # postgres:
  #   image: postgres:15-alpine
  #   container_name: go-fiber-postgres
  #   environment:
  #     POSTGRES_DB: go_fiber_db
  #     POSTGRES_USER: postgres
  #     POSTGRES_PASSWORD: password
  #   ports:
  #     - "5432:5432"
  #   volumes:
  #     - postgres_data:/var/lib/postgresql/data
  #     - ./init.sql:/docker-entrypoint-initdb.d/init.sql  # Optional: initial SQL scripts
  #   networks:
  #     - app-network
  #   restart: unless-stopped

  # redis:
  #   image: redis:7-alpine
  #   container_name: go-fiber-redis
  #   ports:
  #     - "6379:6379"
  #   volumes:
  #     - redis_data:/data
  #   networks:
  #     - app-network
  #   restart: unless-stopped
  #   command: redis-server --appendonly yes

  swagger-ui:
    image: swaggerapi/swagger-ui
    container_name: go-fiber-swagger
    ports:
      - "8081:8080"
    environment:
      SWAGGER_JSON: /docs/swagger.json
      BASE_URL: /swagger
    volumes:
      - ./docs:/docs  # Mount your swagger docs directory
    networks:
      - app-network
    restart: unless-stopped

# volumes:
#   postgres_data:
#   redis_data:

networks:
  app-network:
    driver: bridge