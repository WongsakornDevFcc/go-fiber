version: "3.8"
services:
  api:
    container_name: fiber-api
    ports:
      - 8080:5001
    build:
      context: .
      dockerfile: Dockerfile
    # env_file:
    #   - .env
    # environment:
    #   - PORT=5001
    volumes:
      - ./:/app